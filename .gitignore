# Credentials — never commit these
.credentials.json
*.credentials.json
*.key
*.pem
*.env

# Claude session data — not code, not needed in repo
sessions/
backups/
shell-snapshots/
*.jsonl

# Runtime cache files
mcp-needs-auth-cache.json
policy-limits.json
.last-cleanup

# OS files
.DS_Store
Thumbs.db

# Node
node_modules/
